# Docker Compose File

version: "3.9"

services:
  backend:
    build: .
    container_name: backend
    expose:
      - "8000"

  frontend:
    build: ../frontend
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      - backend