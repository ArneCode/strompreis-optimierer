# Backend
Docstrings are mostly generated by ai. Methods that are generated by ai are marked.

Optimizer compile:
```
cd optimizer
maturin develop --release
cd ..
```
Pull new changes from optimizer:
```
git subtree pull --prefix=optimizer optimizer_py master
```